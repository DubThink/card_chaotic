# immediate
card background fades

# definite, but not immediate
copy/paste for text boxes (with validate?)
image size check

# long-term
line wrapping

clientdefinecard event sends definition + image to server
server creates new or updates source; caches image; sends out def???