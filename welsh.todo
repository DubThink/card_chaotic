# immediate
card background fades

# soon
copy/paste for text boxes (with validate?)
ping clients every x amount of time to keep network state from going stale

# checks
image size check
card validity check?

# long-term
line wrapping
refactor UI to not have _xyz functions by calling super instead
refactor versionedserializable to redirect deserialize() to deserializeFromVersion()
